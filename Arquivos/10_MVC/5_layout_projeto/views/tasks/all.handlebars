<div class="container">
    <h1>Tasks</h1>
    {{#if tasks}}
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Description</th>
                    <th>Status</th>
                </tr>
            </thead>
            <tbody>
                {{#each tasks}}
                    <tr>
                        <td>{{this.title}}</td>
                        <td>{{this.description}}</td>
                        <td><input type="checkbox" {{#if this.done}}checked{{/if}} disabled></td>
                    </tr>
                {{/each}}
            </tbody>
        </table>
    {{else}}
        <p>No tasks found.</p>
    {{/if}}
</div>
